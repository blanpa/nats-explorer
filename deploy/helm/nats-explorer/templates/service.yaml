apiVersion: v1
kind: Service
metadata:
  name: {{ include "nats-explorer.fullname" . }}
  labels: {{- include "nats-explorer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "nats-explorer.selectorLabels" . | nindent 4 }}
