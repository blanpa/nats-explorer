{
  "name": "nats-dev",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "docker compose up -d && echo 'NATS server running on localhost:4222' && echo 'Monitoring: http://localhost:8222'",
    "stop": "docker compose down",
    "seed": "node seed.mjs",
    "simulate": "node simulate.mjs",
    "dev": "docker compose up -d && sleep 2 && node seed.mjs && node simulate.mjs"
  },
  "dependencies": {
    "nats": "^2.28.0"
  }
}
